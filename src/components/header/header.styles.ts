export const headerStyles = {
  root: "fixed top-0 left-0 right-0 z-50",

  bar:
    "relative supports-[backdrop-filter]:backdrop-blur-lg " +
    "border-b border-slate-200 dark:border-slate-800 shadow-lg",

  inner: "container mx-auto px-4 flex h-20 items-center justify-between",
};
