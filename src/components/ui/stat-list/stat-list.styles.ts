// src/components/ui/stat-list/stat-list.styles.ts

import { GRID_COLS, GRID_COLS_MD, GRID_COLS_LG } from "./stat-list.utils";

export const statListStyles = {
  root: "grid gap-6",

  columns: {
    base: GRID_COLS,
    md: GRID_COLS_MD,
    lg: GRID_COLS_LG,
  },
};
